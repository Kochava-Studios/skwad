# List Worktrees

Use the `list-worktrees` MCP tool to list all git worktrees for a repository.

`list-worktrees` parameters:
- `repoPath`: Path to the repository

Based on $ARGUMENTS, fill in the repo path. If not provided, use the current working directory.
