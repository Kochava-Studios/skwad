# Broadcast Message

Use the `broadcast-message` MCP tool to send a message to all other agents in the skwad.

`broadcast-message` parameters:
- `from`: Your agent ID (provided at registration)
- `content`: Message content to broadcast: $ARGUMENTS

The message will be sent to all registered agents except yourself.
