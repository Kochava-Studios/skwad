# Show Markdown

Use the `display-markdown` MCP tool to show a markdown file in Skwad's preview panel.

`display-markdown` parameters:
- `agentId`: Your agent ID
- `filePath`: Absolute path to the markdown file to display

Based on $ARGUMENTS, fill in the file path. If not provided, ask the user which file to display.
